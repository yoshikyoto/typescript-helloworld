{
  "name": "nvapi-client-test",
  "description": "nvapi-clientの動作確認",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^3.5.3"
  }
}
